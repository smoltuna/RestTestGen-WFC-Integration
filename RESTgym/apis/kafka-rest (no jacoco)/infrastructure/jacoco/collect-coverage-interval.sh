#!/bin/bash
# JaCoCo Coverage Collection Script for kafka-rest
# NOTE: JaCoCo not applicable for pre-built binary distribution

# This script is a placeholder for RESTgym template compliance
# Kafka REST Proxy is distributed as pre-built JAR by Confluent Platform
# Code coverage instrumentation is not available

echo "$(date) - JaCoCo coverage collection started (placeholder mode)"
echo "WARNING: Kafka REST Proxy uses pre-built binary - JaCoCo not available"
echo "Use mitmproxy metrics for HTTP interaction coverage instead"

# Keep script running for template compliance
while true; do
    sleep 60
    echo "$(date) - JaCoCo not available for pre-built binary (see classes/README.md)"
done

# Classes Directory

Compiled `.class` files extracted from `publisher-api.jar`.

**Package**: `com.ivanfranchin.publisherapi`

**Location in JAR**: `BOOT-INF/classes/`

These classes are used by JaCoCo for code coverage instrumentation.

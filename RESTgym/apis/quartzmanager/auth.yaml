# WFC Auth configuration for Quartz Manager
# Authentication: JWT Bearer Token via login endpoint

auth:
  - name: admin
    loginEndpointAuth:
      payloadRaw: "username=restapitestteam@gmail.com&password=universe"

authTemplate:
  loginEndpointAuth:
    endpoint: /quartz-manager/auth/login
    verb: POST
    contentType: application/x-www-form-urlencoded
    token:
      extractFrom: body
      extractSelector: /accessToken
      sendIn: header
      sendName: Authorization
      sendTemplate: "Bearer {token}"

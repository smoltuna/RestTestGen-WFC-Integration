# Keycloak API Configuration for RestTestGen + WFC Auth
name: keycloak

specificationFileName: openapi.json

# Server URL - localhost for testing
host: "http://localhost:9094"

# WFC Authentication - OAuth2 via OIDC token endpoint
authFileName: keycloak-auth.yaml
